1) if you want to change current json-data you need to make changes on 
generate_hive_data.py for generating new input-data. Make sure that you
are using the right dataset in the html. pls use python3 for executing the .py!

2) for starting the crime_hiveplot.html you need internet access for the
external scripts.

3) before you open the html in the browser pls type the command:
python3 -m http.server 8888 &
